# Standing Rules

> Durable, user-defined rules that apply across sessions. **Ships empty by design — it holds only rules you state.**
> One rule per section: the rule + the minimum to act on it. Keep "why/when" provenance in an on-demand note, not here.
> A rule only fires if the auto-loaded layer (CLAUDE.md or memory) contains it or a one-line pointer to it.

<!-- Add rules below; delete this comment. Example shape:

## 1. <short imperative rule title>
<one or two lines: the rule + the command / threshold / skip-case needed to follow it>

-->
